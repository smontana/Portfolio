# Disable source maps in production builds for better security and smaller bundle size
GENERATE_SOURCEMAP=false

# Inline runtime chunk for better performance (reduces HTTP requests)
INLINE_RUNTIME_CHUNK=true

# Disable ESLint during production builds (faster builds)
DISABLE_ESLINT_PLUGIN=true

# Enable image optimization
IMAGE_INLINE_SIZE_LIMIT=10000
